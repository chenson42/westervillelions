# Release Notes — v1.18

*← [v1.17](v1.17.md)*

---

## Table of Contents

| Version | Date | Type | Description |
|---------|------|------|-------------|
| [1.18.7](#1187) | 2026-06-24 | Security | Close SSRF in zeffy-meta endpoint, add per-page auth guards to 8 admin pages, fix expired-token cleanup |
| [1.18.6](#1186) | 2026-06-23 | Defect Fix | Donation page restored — Zeffy embeds no longer blocked by CSP, and every campaign card shows an image |
| [1.18.5](#1185) | 2026-05-27 | Security | Replace `xlsx` with `exceljs` in admin export endpoints — production CVE audit is fully clean |
| [1.18.4](#1184) | 2026-05-27 | Infrastructure | Batch dependency bumps + `minimatch` override closes 3 transitive ReDoS CVEs |
| [1.18.3](#1183) | 2026-05-27 | Security | Unlinked-user notification fires once per account, not every sign-in; email-queue retry now gated on `ADMIN_USERS` feature |
| [1.18.2](#1182) | 2026-05-27 | Security | Member/newsletter exports require REPORTS_EXPORT; Next + drizzle-orm CVE patches |
| [1.18.1](#1181) | 2026-05-27 | Defect Fix | Admin events list attending count now includes guests |
| [1.18.0](#1180) | 2026-05-22 | Feature | Dynamic plastic-film drop-off locations admin + Google Maps icons on every drop-off site |

---

<a name="1187"></a>
## 1.18.7 — 2026-06-24

### Security: Hardening from the 2026-06-24 security & test-coverage reviews

**Background:** The monthly security review confirmed all four prior high-severity findings are closed and the production CVE audit is clean, then surfaced three new mediums. This release closes them and fixes a date-anchored test that had started failing.

**Changes:**
- **SSRF closed in the admin `zeffy-meta` endpoint.** The URL check used a bypassable substring match (`url.includes("zeffy.com")`), which would have let an authenticated user make the server fetch arbitrary URLs. It now parses the URL and enforces a strict hostname allowlist (`zeffy.com` / `www.zeffy.com`, HTTPS only), and the endpoint now requires the `campaigns.manage` permission. (The endpoint is best-effort only — Zeffy now blocks server-side fetches — so it returns no metadata in practice.)
- **Defense-in-depth auth guards on 8 admin pages** (announcements, campaigns, events, groups, members, roles, testimonials, users). Each page now re-checks the session and the page's specific feature permission in its own body, rather than relying solely on the admin layout.
- **Expired password-reset tokens are now actually cleaned up.** The cleanup query compared for exact-instant equality instead of "expired on or before now," so it never deleted anything; it now sweeps all expired tokens and runs opportunistically when a new reset is requested.

**Testing:**
- [x] Typecheck, 115 unit tests, and production build pass
- [x] e2e green (donate, cancel-occurrence, write-in-signups, smoke)
- [x] Admin pages still build and route correctly; admins retain access (all features granted)

---

<a name="1186"></a>
## 1.18.6 — 2026-06-23

### Defect Fix: Donation Page Restored

**Problem:** The `/donate` page was broken for the public. Each donation form showed *"This content is blocked. Contact the site owner to fix the issue."* instead of the Zeffy donation form, and the "Donate to Lions Club" campaign card rendered with no image.

**Root Cause:** Two independent defects:
- **Blocked embed:** The donation forms are Zeffy iframes, but our Content-Security-Policy `frame-src` directive never whitelisted `https://www.zeffy.com`, so the browser blocked them. (The "content is blocked" message comes from our own CSP, not from Zeffy.)
- **Missing image:** The "Donate to Lions Club" campaign has no stored image, so the page scraped Zeffy's `og:image` at request time. Zeffy's Cloudflare now returns `403` to all server-side fetches, so the scrape came back empty and the card rendered image-less.

**Fix:**
- Added `https://www.zeffy.com` to CSP `frame-src` so the donation iframes load. Also relaxed `Permissions-Policy` to `payment=(self "https://www.zeffy.com")` so Apple Pay / Google Pay work inside the Zeffy form.
- Removed the now-defunct request-time `og:image` scrape and added a local brand fallback image, so every campaign card always shows an image even when no image is stored. A stored campaign image still takes precedence over the fallback.

**Testing:**
- [x] `/donate` renders every campaign card with a visible image (new e2e: `e2e/donate.spec.ts`)
- [x] Opening a campaign shows the Zeffy donation iframe (`zeffy.com/embed/…`)
- [x] CSP response header includes `https://www.zeffy.com` in `frame-src`
- [x] Production build + typecheck pass

---

<a name="1185"></a>
## 1.18.5 — 2026-05-27

### Security: Replace `xlsx` with `exceljs` in Admin Export Endpoints

**Background:** The 2026-05-27 security and dependencies reviews both flagged `xlsx` (SheetJS) for two unpatched high CVEs — Prototype Pollution (`GHSA-4r6h-8v6p-xvw6`) and ReDoS (`GHSA-5pgg-2g8v-p4x9`). The SheetJS maintainers moved patched releases off the npm registry, so there is no patch path via version bump. Both CVEs primarily affect *parsing* untrusted XLSX input, which neither production endpoint does (they only *write* XLSX), but the package remained the sole reason `pnpm audit --prod` reported high CVEs.

**Changes:**
- Both admin export endpoints (`/api/admin/members/export`, `/api/admin/newsletter/export`) now build their workbooks with [`exceljs`](https://www.npmjs.com/package/exceljs) instead of `xlsx`. The Zeffy column order, headers, sheet names, file names, and content type are unchanged — downstream import into Zeffy is unaffected.
- `xlsx` moved from `dependencies` to `devDependencies`. Two one-off admin import scripts under `scripts/` (`import-roster.ts`, `update-member-details.ts`) still parse vendor `.xlsx` files via `xlsx`; they run only from the admin's local machine against trusted exports, so the residual CVE exposure is bounded to a dev-time path that never ships to production.

**Audit result:** Production `pnpm audit --prod --audit-level=high` is now fully clean. The earlier-today CVE count went from 14 high to 2 high to **0 high**.

---

<a name="1184"></a>
## 1.18.4 — 2026-05-27

### Infrastructure: Batch Dependency Bumps + Minimatch Override

**Background:** The 2026-05-27 dependencies review enumerated 9 safe patch bumps, several safe minor bumps, and 3 transitive `minimatch` ReDoS CVEs that travel through `googleapis`. Today's earlier v1.18.2 already handled the critical-path bumps (`next` 16.1.6 → 16.2.6, `drizzle-orm` 0.45.1 → 0.45.2); this entry catches up the remaining safe ones.

**Production dependencies bumped:**
- `react` / `react-dom` 19.2.3 → 19.2.6
- `@auth/drizzle-adapter` 1.11.1 → 1.11.2
- `date-fns` 4.1.0 → 4.3.0
- `googleapis` 169.0.0 → 172.0.0
- `postgres` 3.4.8 → 3.4.9
- `react-hook-form` 7.71.1 → 7.76.1
- `resend` 6.9.2 → 6.12.4 (closes transitive `uuid` moderate CVE)
- `tailwind-merge` 3.4.0 → 3.6.0

**Dev dependencies bumped:**
- `vitest` / `@vitest/coverage-v8` 4.1.6 → 4.1.7
- `drizzle-kit` 0.31.9 → 0.31.10
- `tsx` 4.21.0 → 4.22.3
- `postcss` 8.5.6 → 8.5.15
- `autoprefixer` 10.4.24 → 10.5.0

**Pnpm override added:**
- `minimatch: ^10` — closes 3 high ReDoS CVEs that travel through `googleapis>google-auth-library>gaxios>rimraf>glob>minimatch`. The dependencies review predicted the `googleapis` 172 bump would resolve these; it didn't, because the vulnerable `minimatch` is 5 layers deep and `gaxios` still pins an older `rimraf>glob`. The override forces the patched minimatch into every dep chain that imports it.

**Removed:**
- `@types/bcryptjs` (deprecated; `bcryptjs` 3.x ships its own type declarations).

**Deferred:** `@marsidev/react-turnstile` 1.4 → 1.5 (needs a changelog read against the Turnstile widget API before bumping), `lucide-react` 0.x → 1.x, `tailwindcss` v3 → v4, `typescript` 5 → 6, `eslint` 9 → 10, and `xlsx` (no patched 0.x release — separate work item to migrate to `exceljs`).

**Audit result:** Production CVE count went from 14 high to 2 high (both unfixable `xlsx`); from 12 moderate to 2 moderate.

---

<a name="1183"></a>
## 1.18.3 — 2026-05-27

### Security: First-Login Guard on Unlinked-User Notification; Feature Gate on Email-Queue Retry

**Background:** Two medium-severity findings from the 2026-05-27 security review.

- **F6:** The "unlinked user signed in" admin notification fired on *every* sign-in by any user without a `memberId` — not just the first one. A user stuck in unlinked state could flood `info@westervillelions.org` with notification emails by signing in repeatedly. The three fire-and-forget DB operations in the JWT callback (lastLoginAt update, auto-link, notify) also raced each other.
- **F5:** `/api/admin/email-queue/retry` checked `session.user.roles?.includes("admin")` instead of `hasFeature()` — the only route in the codebase using that pattern, inconsistent and using stale session data.

**Changes:**
- Restructured the JWT callback: the three DB operations now run sequentially inside a single async block. The unlinked-user notification fires only when `lastLoginAt` was `NULL` at sign-in time (true first-ever login) **and** auto-link didn't resolve the membership. The user's name and email are now HTML-escaped before going into the email body.
- `/api/admin/email-queue/retry` and `/admin/email-queue` page now require `FEATURES.ADMIN_USERS` via `hasFeature()` — consistent with all other admin surfaces. No migration needed; the Admin role already holds this feature.

---

<a name="1182"></a>
## 1.18.2 — 2026-05-27

### Security: Export Endpoints Require REPORTS_EXPORT; CVE Patches for Next.js and drizzle-orm

**Background:** The 30-day code and security reviews independently flagged that `/api/admin/members/export` and `/api/admin/newsletter/export` checked only `auth()` — any logged-in member could download the full active-member roster (name, email, phone, address) or the full newsletter subscriber list. Separately, the dependencies and security reviews flagged that `next` 16.1.6 carried 8 high CVEs (middleware/proxy bypass, SSRF via WebSockets, Server Components DoS) and `drizzle-orm` 0.45.1 carried a SQL-injection CVE via improperly escaped identifiers. All of these are patched upstream.

**Changes:**
- Both export endpoints now require `FEATURES.REPORTS_EXPORT` in addition to a valid session. Members without that feature receive `403 Forbidden`. The Admin role already holds this feature; no migration needed.
- Bumped `next` 16.1.6 → 16.2.6 and `eslint-config-next` 16.1.1 → 16.2.6 (closes 8 high CVEs).
- Bumped `drizzle-orm` 0.45.1 → 0.45.2 (closes the identifier-escape SQLi).
- Corrected the 2026-05-27 code review's MEDIUM-2 finding: `src/proxy.ts` is the Next.js 16 middleware convention (renamed from `middleware.ts`), not dead code. The build output confirms it is registered as `ƒ Proxy (Middleware)`.

---

<a name="1181"></a>
## 1.18.1 — 2026-05-27

### Defect Fix: Admin Events List Attending Count Excluded Guests

**Problem:** On `/admin/events`, the expanded RSVP summary for each event ("✓ N attending · ~ N maybe · ✕ N declined · N total") only counted RSVP rows, not the people each RSVP represented. An RSVP marked "attending" with 2 guests counted as 1, not 3 — so the headline number on the list page understated turnout for any event where guests were allowed and used.

**Root Cause:** The per-event rollup query grouped by status and used `count(*)`, ignoring the `guest_count` column entirely. Other surfaces (the per-event detail page, the public signup endpoint) already counted each RSVP as `1 + guestCount` people; the list view had silently drifted out of sync.

**Fix:** The list page's rollup now sums `guest_count` alongside the row count and adds both into the attending / maybe / declined / total buckets — matching the convention already used on the event detail page (`attending.length + totalGuests`) and the public signup endpoint.

**Testing:**
- [x] Typecheck and production build both pass
- [x] Existing unit tests (115) pass — no test setup creates RSVPs with non-zero `guest_count`, so the change is functionally a no-op for the existing e2e regression suite on `recurring-signup-rollup.spec.ts`

---

<a name="1180"></a>
## 1.18.0 — 2026-05-22

### Feature: Plastic-Film Drop-Off Locations

**Value:** The plastic-film recycling card on `/programs` was a hardcoded reference to a single Pure Roots location. Adding a second host (or correcting a phone number, or updating an entry-instructions line) required a code deploy. The eyeglass drop-off card has always been admin-managed; bringing plastic up to the same model lets the club expand the program without engineering involvement.

#### What's New

- **Admin manager** at `/admin/programs` — a new "Plastic Drop-Off Locations" section directly below the eyeglass section. Same controls: add, edit, toggle active/inactive, delete (with `<ConfirmDialog>`), reorder by sort key.
- **Two extra fields** beyond the eyeglass schema:
  - **Entry instructions** — short free text, e.g. "Enter through the back door." Shows under the address on the public card.
  - **Hours** — short free text, e.g. "Saturdays during the summer."
- **Dynamic public render.** The `/programs` plastic card body now lists every active plastic drop-off location pulled from the database, with the same visual treatment as the glasses card.
- **Empty state.** If no plastic sites are active, the card shows "Locations coming soon — check back shortly or contact us for details." rather than going blank.
- **Two seed sites included.** A first deploy populates the table with Pure Roots Boutique (Westerville, OH; "Enter through the back door.") and the Westerville Farmers Market (COhatch parking lot, 240 S. State St; "Come find us at the Westerville Lions tent."; "Saturdays during the summer"). Both can be edited or removed from the admin UI.

### Enhancement: Drop-Off Location Layout + Google Maps Links

**Value:** On the previous build, the drop-off location block was pinned to the bottom of each program card via `mt-auto`, which left a large empty gap when one card's location list was much shorter than the other's. And neither card surfaced a way to actually navigate to the listed address — visitors had to copy the text into their own maps app.

**Changes:**

- The "Drop-off Locations" block now sits immediately under the intro paragraph on **both** the eyeglass and plastic cards — above "What we accept / cannot accept." The actionable information (where to drop off) leads; the educational lists trail.
- Every drop-off row on **both** cards now shows a small map-pin icon next to the address. Tapping it opens a Google Maps search for that address in a new tab. The link is auto-derived from the existing `address` field — no schema change, no admin work, applies to every existing eyeglass site for free.

#### Permissions

| Feature key | Required for |
|-------------|--------------|
| `announcements.manage` (existing) | Adding, editing, toggling, or deleting plastic drop-off locations. Same key the eyeglass admin uses. No new permission. |

#### New Routes

- `GET /api/admin/programs/plastic-dropoff` — list all plastic drop-off locations (admin only).
- `POST /api/admin/programs/plastic-dropoff` — create a new location.
- `PATCH /api/admin/programs/plastic-dropoff/[id]` — partial update.
- `DELETE /api/admin/programs/plastic-dropoff/[id]` — hard delete.

#### Schema Change

A new table `plastic_dropoff_locations` was added (`name`, `address`, `phone`, `entry_instructions`, `hours`, `is_active`, `sort_order`, timestamps). Migration `0039_plastic_dropoff_locations.sql` is idempotent: `CREATE TABLE IF NOT EXISTS`, and each seed `INSERT` is guarded by `WHERE NOT EXISTS (SELECT 1 ... WHERE name = ...)` so admins can rename or delete seed rows without the migration silently restoring them on the next deploy.
