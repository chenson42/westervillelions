# Release Notes — v1.7

← [v1.6](v1.6.md)

---

## Table of Contents

| Version | Date | Type | Description |
|---------|------|------|-------------|
| [1.7.6](#176) | 2026-04-13 | Enhancement | Responsive admin tables; expandable RSVP summary rows on events |
| [1.7.5](#175) | 2026-04-13 | Enhancement | RSVP remembered on detail page; inline RSVP removed from list pages |
| [1.7.3](#173) | 2026-04-13 | Defect Fix | Restore requiresRsvp gate; redirect /members/events/[id] to public page |
| [1.7.2](#172) | 2026-04-13 | Defect Fix | RSVP always shown on public event detail; members events link to public page |
| [1.7.1](#171) | 2026-04-13 | Feature | Public RSVP on event detail pages with optional guest count |
| [1.7.0](#170) | 2026-04-12 | Feature | Auto-sync board_member role with Board of Directors group membership |

---

<a name="176"></a>
## 1.7.6 — 2026-04-13

### Enhancement: Responsive Admin Tables; Expandable RSVP Summary on Events

**Changes:**
- **Responsive tables** — all 9 admin list pages (events, members, users, groups, roles, announcements, email queue, subscriptions, testimonials) now wrap their tables in `overflow-x-auto` so they scroll horizontally on narrow screens instead of overflowing the layout
- **Expandable RSVP rows** — RSVP-enabled events in the admin events table show a chevron toggle; clicking it reveals a sub-row with attending / maybe / declined counts and a "View all RSVPs →" link. Auto-expanded by default when the event is upcoming and has at least one response

**Files Added:**
- `src/components/admin/event-table-row.tsx` — client component for event row with collapsible RSVP sub-row

**Files Modified:**
- `src/app/(dashboard)/admin/events/page.tsx` — fetch RSVP counts; use EventTableRow
- `src/app/(dashboard)/admin/announcements/page.tsx` — overflow-x-auto wrapper
- `src/app/(dashboard)/admin/email-queue/page.tsx` — overflow-x-auto wrapper
- `src/app/(dashboard)/admin/groups/page.tsx` — overflow-x-auto wrapper
- `src/app/(dashboard)/admin/members/page.tsx` — overflow-x-auto wrapper
- `src/app/(dashboard)/admin/roles/page.tsx` — overflow-x-auto wrapper
- `src/app/(dashboard)/admin/subscriptions/page.tsx` — overflow-x-auto wrapper
- `src/app/(dashboard)/admin/testimonials/page.tsx` — overflow-x-auto wrapper
- `src/app/(dashboard)/admin/users/page.tsx` — overflow-x-auto wrapper

---

<a name="175"></a>
## 1.7.5 — 2026-04-13

### Enhancement: RSVP Remembered on Detail Page; Inline RSVP Removed from List Pages

**Value:** RSVP logic now lives in one place — the event detail page. List pages are read-only with status badges; all RSVP interactions happen on `/events/[id]`.

**Changes:**
- **Event detail page** — fetches the logged-in user's existing RSVP on load, so the buttons show your current response when you return
- **Members events list** — removed inline 3-button RSVP widget; replaced with a colored status badge (green/yellow/gray) and an "RSVP" / "Update RSVP" link to the detail page for events that require RSVP

**Files Modified:**
- `src/app/events/[id]/page.tsx` — fetch user RSVP; pass initialStatus/initialGuestCount to PublicRsvpForm
- `src/app/members/events/page.tsx` — remove EventRsvp widget; add status badge + RSVP link

---

<a name="174"></a>
## 1.7.4 — 2026-04-13

### Enhancement: RSVP Remembered on Detail Page; Inline RSVP Removed from List Pages

**Value:** RSVP logic now lives in one place — the event detail page. List pages are read-only with status badges; all RSVP interactions happen on `/events/[id]`.

**Changes:**
- **Event detail page** — fetches the logged-in user's existing RSVP and passes `initialStatus` and `initialGuestCount` to the RSVP form, so the previously selected response is shown on return visits
- **Members events list** — removed inline 3-button RSVP widget; replaced with a colored status badge (green/yellow/gray) and an "RSVP" / "Update RSVP" link to the detail page for events that require RSVP

**Files Modified:**
- `src/app/events/[id]/page.tsx` — fetch user RSVP; pass initialStatus/initialGuestCount to PublicRsvpForm
- `src/app/members/events/page.tsx` — remove EventRsvp widget; add status badge + RSVP link

---

<a name="173"></a>
## 1.7.3 — 2026-04-13

### Defect Fix: requiresRsvp Gate Restored; Event Detail Pages Made Public

**Problem 1:** v1.7.2 removed the `requiresRsvp` gate entirely — RSVP form was showing on every event regardless of whether "Requires RSVP" was checked.
**Fix:** Restored `event.requiresRsvp &&` gate on the RSVP form in the public event detail page.

**Problem 2:** `/members/events/[id]` still required login; the old auth-protected page was not removed.
**Fix:** Replaced the members event detail page with a redirect to `/events/[id]`, so any link or bookmark to that URL lands on the public page without requiring login.

**Problem 3:** `/events/[id]` URLs required login due to a missing public path prefix in `proxy.ts`. The middleware had an `/^\/events/` protection rule and its `publicPaths` list only matched `/events` exactly — not `/events/ce4b346b...` subpaths.
**Fix:** Added `/events/` as a public prefix and removed the `/^\/events/` protection rule (members events are already covered by the `/^\/members/` rule).

**Files Modified:**
- `src/app/events/[id]/page.tsx` — restored `requiresRsvp` gate on RSVP form
- `src/app/members/events/[id]/page.tsx` — replaced with redirect to `/events/[id]`
- `src/proxy.ts` — added `/events/` public prefix; removed `/^\/events/` protection rule

---

<a name="172"></a>
## 1.7.2 — 2026-04-13

### Defect Fix: RSVP Always Shown; Members Events Links to Public Page

**Problem 1:** RSVP form only appeared when "Requires RSVP" was checked on the event — but the intent is for anyone with a link to always be able to RSVP.
**Fix:** Removed the `requiresRsvp` gate on the public event detail page — RSVP form always shows.

**Problem 2:** Links on the members events list pointed to `/members/events/[id]` (auth-required) instead of the public `/events/[id]`.
**Fix:** Updated all event links on the members events list to `/events/[id]`.

**Files Modified:**
- `src/app/events/[id]/page.tsx` — removed `requiresRsvp` gate on RSVP form
- `src/app/members/events/page.tsx` — links updated to `/events/[id]`

---

<a name="171"></a>
## 1.7.1 — 2026-04-13

### Feature: Public RSVP on Event Detail Pages with Optional Guest Count

**Value:** Anyone with an event link can now RSVP without creating an account. Private events are still hidden from the public events listing, but anyone with the direct link can view and RSVP — useful for sharing invites externally.

#### What's New

- **No login required to RSVP** — anonymous users provide name + email; returning anonymous users update their RSVP by matching email
- **Private events accessible via direct link** — `/events/[id]` no longer requires `isPublic = true`; the listing page still filters to public only
- **Guest count field** — new per-event `allowGuestCount` setting; when enabled, the RSVP form shows a guest count input for "Attending" responses
- **Logged-in members** see the 3-button RSVP interface with optional guest count
- **Anonymous users** see a name + email form with status buttons and optional guest count, followed by a confirmation message
- **Admin event form** — new "Allow guest count" checkbox in the Settings section
- **Admin event detail** — anonymous RSVPs appear in the RSVP table with a `(guest)` tag

#### Files Added
- `drizzle/migrations/0026_public_rsvp.sql` — adds `rsvp_name`, `rsvp_email` to `event_rsvps`; makes `user_id` nullable; adds `allow_guest_count` to `events`
- `src/components/public/public-rsvp-form.tsx` — client component handling both logged-in and anonymous RSVP

#### Files Modified
- `src/lib/db/schema.ts` — `eventRsvps.userId` nullable; added `rsvpName`, `rsvpEmail`; added `allowGuestCount` to `events`
- `src/app/api/events/[id]/rsvp/route.ts` — POST/DELETE now support unauthenticated requests
- `src/app/events/[id]/page.tsx` — removed `isPublic` filter; renders `PublicRsvpForm` when `requiresRsvp` is true
- `src/components/admin/event-form.tsx` — added `allowGuestCount` checkbox
- `src/app/api/admin/events/route.ts` — `allowGuestCount` saved on create
- `src/app/api/admin/events/[id]/route.ts` — `allowGuestCount` saved on update
- `src/app/(dashboard)/admin/events/[id]/page.tsx` — left join for anonymous RSVPs; `(guest)` tag

---

<a name="170"></a>
## 1.7.0 — 2026-04-12

### Feature: Auto-Sync board_member Role with Board of Directors Group

**Value:** Board members previously had to be manually given the `board_member` role separately from being added to the Board of Directors group. This caused the role to drift out of sync. Now it's automatic — adding someone to the board assigns the role, removing them revokes it.

#### What's New

- **Automatic role assignment** — When a member is added to the "Board of Directors" group, their linked user account is automatically assigned the `board_member` role (if they have one)
- **Automatic role removal** — When a member is removed from the group, the `board_member` role is revoked
- **Sync endpoint** — `POST /api/admin/groups/sync-board-role` assigns/removes the role for all current board members in bulk
- **Sync button** — "Sync Board Member Role" button appears on the Board of Directors group admin page for manual re-sync
- **One-time sync ran** — 9 board members were assigned the `board_member` role on deploy; 3 already had it; 0 removed

#### Files Added
- `src/app/api/admin/groups/sync-board-role/route.ts` — bulk sync endpoint (admin only)
- `src/components/admin/sync-board-role-button.tsx` — client component for sync button
- `scripts/sync-board-role.ts` — reusable one-time sync script

#### Files Modified
- `src/app/api/admin/groups/[id]/members/route.ts` — added `syncBoardMemberRole()` helper on POST/DELETE
- `src/app/(dashboard)/admin/groups/[id]/page.tsx` — renders sync button when viewing Board of Directors
