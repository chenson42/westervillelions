# Release Notes — v1.4

← [v1.3](v1.3.md)

---

## Table of Contents

| Version | Date | Type | Description |
|---------|------|------|-------------|
| [1.4.7](#147) | 2026-04-06 | Enhancement | UX quick hits: nav, hero CTA, contact form friction |
| [1.4.6](#146) | 2026-04-06 | Enhancement | Image compression, dead code removal, donate page polish |
| [1.4.5](#145) | 2026-04-06 | Enhancement | WCAG AA contrast fix: hero eyebrow labels |
| [1.4.4](#144) | 2026-04-06 | Enhancement | Performance and accessibility improvements |
| [1.4.3](#143) | 2026-04-06 | Enhancement | Google Analytics integration |
| [1.4.2](#142) | 2026-04-06 | Enhancement | SEO improvements: robots.txt, canonical URLs, OG tags, meta descriptions, card/button consistency |
| [1.4.1](#141) | 2026-04-06 | Enhancement | Homepage event cards styled to match service area cards |
| [1.4.0](#140) | 2026-04-06 | Feature | Google Groups sync, member portal groups pages, and member data enrichment |

---

<a name="147"></a>
## 1.4.7 — 2026-04-06

### Enhancement: UX Quick Hits — Nav, Hero CTA, Contact Form

**Value:** Reduces friction for first-time visitors at three high-visibility touchpoints.

**Changes:**
- **Nav:** Renamed "Connect" → "Contact" — removes ambiguity; visitors looking for contact info now find it immediately
- **Hero CTA:** Secondary button changed from "Learn More" → "/about" to "Attend a Meeting" → "/contact" — sends warm leads to the most actionable next step instead of a dead-end About page
- **Member Login:** De-emphasized from a prominent blue button to a subtle text link — frees up visual weight for conversion-focused nav elements without removing access for members
- **Contact form:** Removed "Subject" field — unnecessary friction; API defaults subject to "General Inquiry" to preserve the `notNull` database constraint

**Files Modified:**
- `src/components/layout/header.tsx` — "Connect" → "Contact", Member Login styling
- `src/app/page.tsx` — hero secondary CTA text and href
- `src/components/contact-form.tsx` — Subject field removed
- `src/app/api/contact/route.ts` — subject defaulted to "General Inquiry"

---

<a name="146"></a>
## 1.4.6 — 2026-04-06

### Enhancement: Image Compression, Dead Code Removal, Donate Page Polish

**Value:** Faster page loads (hero image was 791KB with no Next.js optimization), leaner codebase with no orphaned files.

**Image Compression (2.2MB → 908KB, −59%):**
- `hero-bg.jpg`: 791KB → 429KB — highest-impact win; this is a CSS background with no Next.js optimization
- `event-wine-with-lions.jpg`: 436KB → 346KB (deleted; image also removed from DB)
- `service-youth.jpg`: 389KB → 40KB (served via Next.js Image, which adds WebP on top)
- `service-humanitarian.jpg`: 363KB → 34KB
- `service-community.jpg`: 276KB → 25KB

**Dead Code Removed:**
- `src/components/home/instagram-grid.tsx` — dropped feature, never imported
- `src/lib/utils.ts` — shadcn boilerplate, never imported anywhere
- `public/images/event-wine-with-lions.jpg` — orphaned image
- 11 one-off operational scripts in `scripts/` (board position fixes, email sync, photo seeding, etc.) — completed tasks, no longer needed

**Donate Page Design System Fixes:**
- "Thank You" block: `bg-blue-50 rounded-xl` → `bg-lions-blue/5 rounded-2xl`
- Impact cards: `rounded-lg` → `rounded-2xl`

**Files Deleted:**
- `public/images/event-wine-with-lions.jpg`
- `src/components/home/instagram-grid.tsx`
- `src/lib/utils.ts`
- `scripts/add-event-photo.ts`, `check-board-members.ts`, `fix-board-positions.ts`, `fix-remaining-emails.ts`, `list-board-members.ts`, `seed-board-photos.ts`, `sync-member-emails.ts`, `update-board-positions.ts`, `update-emails-from-roster.ts`, `update-event-visibility.ts`

**Files Added:**
- `docs/ux-audit-2026-04-06.md` — full UX/conversion audit (18 prioritized issues)

**Files Modified:**
- `public/images/hero-bg.jpg`, `service-youth.jpg`, `service-community.jpg`, `service-humanitarian.jpg` — compressed
- `src/app/donate/page.tsx` — design system fixes

---

<a name="145"></a>
## 1.4.5 — 2026-04-06

### Enhancement: WCAG AA Contrast Fix

**Problem:** Hero eyebrow labels (`text-lions-gold` on `bg-lions-blue`) had a 3.2:1 contrast ratio, failing WCAG AA which requires 4.5:1 for normal text.
**Fix:** Changed to `text-white/80` (~10:1 contrast ratio). Preserves visual hierarchy — the slightly dimmed white eyebrow reads as secondary to the bold white H1 below it.

**Files Modified:**
- `src/app/about/page.tsx`, `contact/page.tsx`, `donate/page.tsx`, `events/page.tsx`, `join/page.tsx`, `mission/page.tsx`

---

<a name="144"></a>
## 1.4.4 — 2026-04-06

### Enhancement: Performance & Accessibility

**Value:** Faster LCP, eliminated layout shift, and improved keyboard/screen reader experience.

**Performance:**
- Deleted unused `public/images/logo.png` (2.3 MB)
- Added `<link rel="preload">` for hero background image — LCP improvement of ~1–2s
- Added `priority` prop to first service card image (above-the-fold eager loading)
- Converted event `<img>` tags to `next/image` `<Image>` with fixed-height containers — eliminates CLS and enables WebP optimization for local images

**Accessibility:**
- Added skip-to-main-content link (visible on focus, for keyboard users)
- Wrapped page content in `<main id="main-content">` landmark element
- Added `aria-label` to "Learn More" hero button
- Added `aria-label` to "See All Events" link

**Files Modified:**
- `src/app/layout.tsx` — preload link, skip link, `<main>` wrapper
- `src/app/page.tsx` — aria-label on "Learn More", priority on first ServiceCard
- `src/components/home/service-card.tsx` — added optional `priority` prop
- `src/components/home/featured-content.tsx` — `<img>` → `<Image>`, aria-label on "See All Events"
- `src/app/events/page.tsx` — `<img>` → `<Image>`

**Files Deleted:**
- `public/images/logo.png` — 2.3 MB unused asset
- `public/images/placeholder.txt`

---

<a name="143"></a>
## 1.4.3 — 2026-04-06

### Enhancement: Google Analytics

**Value:** Site traffic and user behavior can now be tracked in Google Analytics.

**Changes:**
- Added GA4 tag (`G-RC3NVC28GC`) via `next/script` with `afterInteractive` strategy — no render blocking
- Covers all pages site-wide (public and member portal)

**Files Modified:**
- `src/app/layout.tsx` — Added two `<Script>` blocks for gtag loader and config

---

<a name="142"></a>
## 1.4.2 — 2026-04-06

### Enhancement: SEO & UI Consistency

**Value:** Improved search engine visibility and consistent visual design across all pages.

#### SEO
- **`robots.ts`** — Created; blocks `/members`, `/admin`, `/api` from crawlers; points to sitemap
- **Canonical URLs** — Added `alternates.canonical` to all public pages (homepage, about, events, mission, donate, contact, join)
- **Events page OG tags** — Added `og:title`, `og:description`, `og:url`, `og:image` (was missing entirely)
- **OG image** — Root layout updated from 250×80 logo to 1200×630 hero image; Twitter card upgraded to `summary_large_image`
- **Meta descriptions** — Shortened root layout (~168→154 chars), homepage (~164→151 chars), donate (~215→155 chars) to stay within 160-char limit
- **Duplicate heading** — Removed redundant "Upcoming Events" H2 on events page (H1 in hero already covers it)

#### UI Consistency
- Cards unified to `rounded-2xl` + `shadow-lg hover:shadow-xl transition transform hover:-translate-y-1` for interactive cards
- Non-interactive cards use `rounded-2xl shadow-sm`
- Empty states standardized to `bg-gray-50 rounded-2xl p-10 text-center text-gray-500`
- Hero buttons changed from `rounded-full` to `rounded-lg` throughout
- Mission page card styles unified (removed blue-tinted background variant)
- Donate page campaign cards: removed one-off `border-2 border-lions-gold`
- Member portal quick-link cards: removed errant `border-2 border-lions-blue` on directory card
- Birthday widget: removed nested inner border
- Announcement links: `text-xs` → `text-sm` to match "See All Events" link

**Files Added:**
- `src/app/robots.ts` — Crawler rules and sitemap pointer

**Files Modified:**
- `src/app/layout.tsx` — OG image, Twitter card type, meta description
- `src/app/page.tsx` — Canonical, og:url, shortened description
- `src/app/events/page.tsx` — OG tags, canonical, removed H2
- `src/app/about/page.tsx` — Canonical
- `src/app/mission/page.tsx` — Canonical, card styles
- `src/app/donate/page.tsx` — Canonical, shortened description, card styles
- `src/app/contact/page.tsx` — Canonical
- `src/app/join/page.tsx` — Canonical
- `src/app/members/page.tsx` — Quick-link border fix, birthday widget border fix
- `src/app/members/groups/page.tsx` — Card styles, empty state
- `src/app/members/groups/[id]/page.tsx` — Card styles, empty state
- `src/app/members/events/page.tsx` — Card styles, empty states
- `src/components/home/featured-content.tsx` — Announcement link text size
- `CLAUDE.md` — Added UX Guidelines section

---

<a name="141"></a>
## 1.4.1 — 2026-04-06

### Enhancement: Homepage Event Card Style

**Value:** Event cards on the homepage now match the visual style of the "Our Service Areas" cards for a more consistent look.

**Changes:**
- Removed blue left accent border from event cards
- Applied `rounded-2xl`, `shadow-lg`, hover lift (`hover:shadow-xl`, `hover:-translate-y-1`), and `p-8` padding to match service cards
- Changed event title color from `text-gray-900` to `text-lions-blue`
- Increased image height from `h-40` to `h-48`

**Files Modified:**
- `src/components/home/featured-content.tsx` — Updated `NextEventCard` article styles

---

<a name="140"></a>
## 1.4.0 — 2026-04-06

### Feature: Google Groups Sync, Member Portal Groups, and Member Data Enrichment

**Value:** Group email addresses (e.g. `board@westervillelions.org`) now stay in sync with portal membership automatically. All active members are synced to `club@westervillelions.org` whenever membership changes. Members can browse groups and contact them by email from the portal. Member records now include full birthdates, addresses, gender, and spouse names from the LCI export.

---

#### Google Groups Sync

- **OAuth2 authentication** via a dedicated `api@westervillelions.org` service account using a refresh token — no service account key required
- Each portal group can be assigned an optional **email prefix** (e.g. `board` → `board@westervillelions.org`)
- Admin group edit form: email prefix input with `@westervillelions.org` suffix, **Sync Now** button, last-synced timestamp, and inline error display
- Sync creates the Google Group if it doesn't exist, then adds/removes members to match the portal roster
- **Sync reports changes**: toast notification lists added and removed email addresses (or confirms "already up to date")
- `POST /api/admin/groups/[id]/sync` — admin-only manual sync endpoint

#### Club-Wide Sync (`club@westervillelions.org`)

- All active members are synced to `club@westervillelions.org` automatically whenever a member is created, updated, or deleted
- **Sync club@** button on the admin Members page for manual full sync
- Sync report shows added/removed emails or confirms no changes needed
- `POST /api/admin/members/sync-club` — admin-only manual sync endpoint

#### Member Portal Groups Pages

- `/members/groups` — lists all groups with name, description, and contact email (if set)
- `/members/groups/[id]` — group detail page with prominent contact email card and member list with profile pictures
- **Groups** quick-link added to the member portal home (now 4 cards)

#### Member Data Enrichment

- `gender` and `spouse_name` columns added to the `members` table
- `pnpm db:update-member-details` command imports full addresses, complete birthdates (with year), gender, and spouse names from the LCI member detail export
- 47 members updated with complete data from the February 2026 LCI export
- Birthday display on the member portal now correctly parses full `YYYY-MM-DD` dates

---

#### Files Added

- `src/lib/google-groups.ts` — `syncGoogleGroup()` and `syncClubMembersList()` utilities
- `src/app/api/admin/groups/[id]/sync/route.ts` — group sync endpoint
- `src/app/api/admin/members/sync-club/route.ts` — club sync endpoint
- `src/components/admin/sync-club-button.tsx` — Sync club@ button component
- `src/app/members/groups/page.tsx` — member portal groups listing
- `src/app/members/groups/[id]/page.tsx` — member portal group detail
- `scripts/update-member-details.ts` — LCI member detail import script
- `drizzle/migrations/0020_groups_google_sync.sql` — email prefix + sync columns on groups
- `drizzle/migrations/0021_members_gender_spouse.sql` — gender + spouse_name on members

#### Files Modified

- `src/lib/db/schema.ts` — added `emailPrefix`, `googleGroupSyncedAt`, `googleGroupSyncError` to groups; `gender`, `spouseName` to members
- `src/components/admin/group-form.tsx` — email prefix input, Sync Now button, sync reporting
- `src/app/(dashboard)/admin/groups/[id]/page.tsx` — pass new fields to GroupForm
- `src/app/api/admin/groups/[id]/route.ts` — save emailPrefix on group update
- `src/app/(dashboard)/admin/members/page.tsx` — Sync club@ button in header
- `src/app/api/admin/members/route.ts` — fire-and-forget club sync on member create
- `src/app/api/admin/members/[id]/route.ts` — fire-and-forget club sync on member update/delete
- `src/app/members/page.tsx` — Groups quick-link card added
- `package.json` — `db:update-member-details` script entry
