# 2026-08-12 — PII Scrub for Public Release

**Trigger:** The private repo is being retained as an archive; the treasurer is starting a fresh
public repository from a scrubbed copy of the working tree. This is an ad hoc scrub, not a
scheduled 30-day security review (though it satisfies that review's PII-sweep scope for this
cycle — see `docs/reviews/log.md`). Git history is out of scope; only the working tree needed
to be clean.

## Scope and policy

Ban: personal email addresses, phone numbers, home/street addresses, and anything tying a named
non-officer person to money, dues, health, or personal circumstances. Allow: club-domain
addresses (`board@`, `club@`, `info@`, `treasurer@`, `noreply@westervillelions.org`), officer
names in a governance context, and `example.com`/`example.invalid`/`*.test` in fixtures. Full
policy is now codified in `CLAUDE.md` → *No Personal Data in the Repository* and enforced going
forward by `/pre-push`'s Personal Data Sweep step (both added in commit `7d521f6`, prior to this
scrub).

## What was found and removed

**9 personal email addresses, ~73 occurrences**, across migrations, scripts, docs, and
work-logs — a mix of `gmail.com`, `outlook.com`, and `att.net` addresses belonging to club
officers and members, including one `+alias` variant used for QA testing. Deliberately not
re-listed here in cleartext (a doc that says "we removed address X" would just reintroduce X into
the public repo it's describing).

**Two additional findings from a follow-up sweep**, not on the original address list:
- A real (non-officer) member's name used twice in `docs/work-log/2026-07-20-ledger-quicken-seed.md`,
  `docs/treasurer-todo.md`, and `scripts/import-quicken-ledger.ts`, tied to two deleted test dues
  rows — replaced with "a family-rate member" / "a family-rate member's name" throughout.
- A single line in `docs/work-log/2026-07-21-impact-gift-public-note.md` naming three 2025
  scholarship recipients (likely minors) tied to specific scholarship gift amounts — replaced with
  "three named individuals — see treasurer's records."

No phone numbers or street/home addresses were found outside the public glasses/plastics
drop-off business directory (`drizzle/migrations/0034_glasses_dropoff_refresh.sql` — organizational
locations with public phone numbers, not personal data) and existing synthetic test fixtures.

## The hard part: migrations that hardcode a person's email

Three migrations used a real address in a `WHERE` clause to grant a role or set billing data:

- **`0040_dues_tracking.sql`** — granted the `treasurer` role to two named people by email.
- **`0069_ledger_budget_permissions.sql`** — granted `budget_committee` to one named person by
  email.
- **`0043_dues_family_members.sql`** — set `dues_category = 'family'` on three named members by
  email. This one doesn't grant access; it's billing data.

**Approach chosen: environment-variable substitution, done in the migration runner.**
`drizzle/run-migrations.mjs` (read first, per the task's own instruction — it has no templating
today, just `readFileSync` + `sql.unsafe(content)`) now does simple string substitution before
executing each file: any occurrence of the literal token `{{SEED_ADMIN_EMAIL}}` is replaced with
`process.env.SEED_ADMIN_EMAIL` (a single quote in the value is escaped so it can't break out of
the SQL string literal). Left unset, it substitutes to `''`, and `WHERE u.email = ''` matches no
real user — a guaranteed no-op, same idempotency guarantee the migrations already had.

`0040` and `0069` were rewritten against this token. `0040` was also extended to grant `admin`,
not just `treasurer` — there was previously no migration anywhere in the repo that could bootstrap
a fresh install's first admin (the existing production admin was granted by hand, outside any
migration). This closes that gap: a fresh install now becomes usable by setting
`SEED_ADMIN_EMAIL` in `.env.local` after a first sign-in and re-running `pnpm db:migrate`,
documented in `SETUP.md` step 7, `.env.example`, and `CLAUDE.md` → Environment Variables.

`0043` was **not** parameterized. It isn't an access grant, and the recommended alternative in the
task ("drop the person-specific seed and document the alternative") fit better here: production
already carries the `dues_category = 'family'` values this migration set (removing the seed
doesn't undo prior runs), and the club already has a purpose-built admin UI for this exact field
(`src/components/admin/dues-category-control.tsx`, `PATCH /api/admin/dues/[memberId]/category`).
The file is kept only so migration numbering stays contiguous, replaced with a comment and a
harmless `SELECT 1;`.

Logged as `DECISION-089` in `docs/decisions.md`.

## Scripts

Several one-off scripts under `scripts/` resolved an actor/creator user by one of the same
hardcoded personal emails, to attribute `recorded_by_user_id`, `created_by`, or an audit-log actor.
All now read `process.env.SCRIPT_OPERATOR_EMAIL` and throw a clear error if it's unset — these are
manually invoked treasury/admin scripts, not something a fresh install runs unattended, so
requiring the operator to set an env var (documented in `CLAUDE.md`) is the right failure mode
rather than a silent no-op:
`add-kroger-rewards-category.ts`, `backfill-dues-ledger.ts`,
`close-out-historical-acknowledgments.ts`, `deactivate-unused-categories.ts`,
`import-quicken-ledger.ts`, `june-close-correction.ts`, `port-ledger-dev-to-prod.ts`,
`add-2026-27-meeting-schedule.ts`, `add-bmx-volunteer-events.ts`.

Two roster scripts (`import-roster.ts`, `sync-roster.ts`) special-cased one real household's
shared email address to give both members a distinct login. This isn't an access grant and a
fresh install's own roster file won't have this specific case, so it now reads three optional env
vars (`SHARED_HOUSEHOLD_EMAIL`, `_ALT`, `_ALT_FIRST_NAME_MATCH`) and is a no-op — not a hard
error — when they're unset.

## Verification

- `git grep -InE "[a-zA-Z0-9._%+-]+@(gmail|hotmail|msn|att|yahoo|sbcglobal|aol|insight|live|outlook|copper|comcast|icloud|me)\.[a-z.]+"` — **clean, zero hits** (also fixed two illustrative `someone@gmail.com` placeholders in `CLAUDE.md` and `.claude/agents/database-admin.md` to `someone@example.com` so the literal sweep command returns nothing, even though those were never real addresses).
- `pnpm exec tsc --noEmit` — clean.
- `pnpm test` — **1466 passed** (78 files), no regressions.
- `pnpm build:only` — production build passes, full route list unchanged.
- `pnpm db:migrate` run twice against DEV (`PROD_DATABASE_URL` explicitly unset for both runs;
  never touched in `.env.local`) — both succeeded; second run produced the identical set of
  idempotency NOTICEs (82) as the first, confirming a true no-op re-run. No email was sent
  (`RESEND_API_KEY` untouched, still blank) and no ledger transactions were created.

## Left in place under the ALLOW rules

- Club-domain addresses (`board@`, `club@`, `info@`, `treasurer@`, `noreply@westervillelions.org`)
  — organizational, already public, load-bearing constants.
- Officer names in governance context — e.g. "Chris Henson," "James Shively" as the two named
  treasurers in historical work-logs and `docs/decisions.md`; "Miriam Reinhoudt" as the secretary
  consulted on a meeting-schedule correction in `scripts/add-2026-27-meeting-schedule.ts`. These
  are public via the club's own website and aren't paired with contact details anymore.
  `0022_testimonials.sql`'s named testimonial author was already policy-compliant and untouched.
  A GitHub handle (`chenson42`, no `@domain`) remains in
  `.claude/agents/deployment-engineer.md` and `docs/reviews/2026-06-24-retrospective.md` — it's an
  account identifier used to explain a Vercel/GitHub deploy-blocking incident, not an email
  address, and is already public via git history (out of scope for this scrub).
  `docs/work-log/2026-06-24-dues-tracking.md`'s historical note that "repo is private" was left
  as-is — it's a timestamped snapshot of a past decision, not a claim about the repo's status
  today.
- Business/organizational phone numbers and addresses in the public glasses/plastics drop-off
  directory (`0034_glasses_dropoff_refresh.sql`) — these are public business locations, not
  personal data.
- `example.com` / `example.invalid` / `*.test` addresses already in tests and fixtures — untouched.

## Financial data pass — 2026-08-12

**Trigger:** Follow-up to the pass above, same public-release effort, scoped to MONEY and DONORS
rather than PII generally: real dollar figures tied to the club's actual books, and individual
(non-organization) identities tied to gifts. Working tree only, same as before.

### Files deleted outright

**Club governance docs** (per explicit instruction — archived in the private repo, not code):
`docs/board-motions.md`, `docs/treasurer-todo.md`, `docs/club-documents/` (the welcome-packet
source), `docs/2026-07-29-budget-actuals-mapping-and-category-cleanup.md`,
`docs/2026-07-29-clean-fy2025-plan.md`.

**One-off scripts whose entire content was real embedded budget/transaction data** — stripping
the numbers would leave a shell that's neither useful nor honest, so deleted rather than gutted,
matching the treatment named explicitly for two of them:
- `scripts/insert-fy2025-budget.ts`, `scripts/split-event-costs.ts` — named directly in the task.
- `scripts/insert-fy2025-admin-budget.ts` — explicit sibling of `insert-fy2025-budget.ts` ("the
  companion... which did the Foundation Charitable fund"), same character: a hardcoded table of
  every FY2025 Club Administrative budget line.
- `scripts/enter-fy2025-approved-budget.ts`, `scripts/apply-fy2026-budget-review.ts`,
  `scripts/merge-club-budget-categories.ts`, `scripts/june-close-correction.ts` — each is a
  one-off, already-applied budget-entry/correction script whose only content is a hardcoded table
  of real category names + real cents amounts (some with real board-facing "Notes & Assumptions"
  prose quoting real totals). `apply-fy2026-budget-review.ts` also named a real, non-donor
  individual (a budget-committee member) reviewing the draft budget by email — out of scope on
  its own per the governance-context rule, but moot once the file was deleted for its embedded
  figures.
- `scripts/add-arborfest-cause-line.ts`, `scripts/backfill-gates-checks.ts` — small, single-purpose
  scripts whose entire job was writing/matching one specific real dollar amount.
- `scripts/render-welcome-packet.sh` — not itself financial, but its only job was rendering
  `docs/club-documents/welcome-packet-2026-27.html`, which is gone; a script for a deleted source
  file is dead weight, so it went too.

### Scripts scrubbed in place (kept — general-purpose or mostly non-financial logic)

`scripts/fix-ledger-categories.ts`, `scripts/backfill-check-numbers.ts`,
`scripts/backfill-dues-ledger.ts`, `scripts/close-out-historical-acknowledgments.ts`,
`scripts/port-ledger-dev-to-prod.ts`, `scripts/import-quicken-ledger.ts`,
`scripts/add-kroger-rewards-category.ts`, `scripts/reset-fy2026-budget.ts`,
`scripts/rehome-misc-actuals.ts`, `scripts/seed-fy2026-foundation-budget.ts` — real dollar
comments and hardcoded `_CENTS` safety-check/matching constants replaced with placeholders
(`999_999`-style values, comments updated to match). All are historical, already-applied,
never-safe-to-rerun scripts (several say so explicitly in their own headers), so a placeholder
constant costs nothing — the assertion it drives will never run against real data again.

**Individual names found tied to real money in scripts, redacted:**
- `scripts/import-quicken-ledger.ts` — `"A J Westlund"` in a test-party matching array, replaced
  with a synthetic name matching its neighbors' style; `"Howard Baum"` and `"Jane Enneking"` in
  live cause-matching logic (`ci(payeeRaw, "howard baum")` etc., real members reimbursed for
  recycling-program supplies) replaced with `"a volunteer"` / `"a second volunteer"` plus a
  comment explaining the redaction, since this script is historical/frozen and the exact match
  string will never matter again.

### Docs (the bulk of the pass)

Ran in 5 parallel passes (3 work-log batches covering all ~58 flagged `docs/work-log/*.md` files,
one for `docs/release-notes/*.md`, one for scripts) plus a personal pass over `CLAUDE.md`,
`docs/backlog.md`, `docs/decisions.md`, `docs/features/the-ledger-accounting.md`,
`docs/ux-audit-2026-04-06.md`, `docs/reviews/2026-06-27-security.md`, and two work-logs needing
special care for donor identities (`2026-07-20-ledger-quicken-seed.md`,
`2026-07-21-impact-gift-public-note.md`) plus a dues-heavy one
(`2026-08-12-dues-reminder-emails.md`). Every real dollar figure — budget lines, fund/account
balances, transaction amounts, giving totals, dues rates, reconciliation numbers — was either
rewritten out of the sentence or replaced with a placeholder (`$X,XXX.XX` or an obviously-fake
round number), keeping tables, structure, and the surrounding reasoning intact. Roughly 60 work-log
and doc files were touched; several dozen more were checked and needed no change (their only `$`
hits were `$0`, IRS/legal thresholds, or already-synthetic test/QA data).

**Individual names found tied to real money in docs, redacted (organizations kept):**
- **A J Westlund** — a real member's name used twice in `2026-07-21-impact-gift-public-note.md` as
  a temporary member-linkage artifact for e2e testing (same pattern flagged by the task itself);
  also fixed the identical pattern in `2026-07-21-impact-cause-drilldown.md` ("Howard Baum").
- **Howard Baum, Kris Thompson, Miriam Reinhoudt, Jeff Reschke** — real members/donors tied to
  specific real ledger transactions in `2026-07-20-ledger-quicken-seed.md`'s treasurer-review flag
  list; replaced with "an individual member" / "a donor," transaction detail kept.
  Batch agents separately found and fixed **Arden Heckman, Emilie Clark, Imani Akita** (three real
  scholarship recipients) in `2026-07-28-fy2026-foundation-budget-seed.md`, **Jane Enneking** (an
  expense reimbursement) in `2026-07-30-activity-fund-budget-cleanup.md`, and **Don Niebling** (a
  real check/income transaction, 8 occurrences) in `2026-07-20-ledger-dashboard.md` and
  `2026-07-21-ledger-check-number.md`.
- **Howard Baum, Debbie Bennati, Gary Bix** — real members used as QA test subjects for real dues
  reminders/partial-payment scenarios in `2026-08-12-dues-reminder-emails.md`; redacted to generic
  descriptors, real dues rates ($120/$96) and live counts scrubbed from the same file.
- Real dues rates ($120 individual / $96 family) were scrubbed from every narrative doc mention
  (work-logs, `decisions.md`, `docs/features/the-ledger-accounting.md`) but **left in place** in
  `src/lib/db/schema.ts`'s comment and in migration `0040_dues_tracking.sql`'s literal seed
  values — those are functional, load-bearing configuration the app needs to seed the club's real
  dues rate correctly, the same category as the `reserveWarnThresholdCents`/
  `disbApprovalThresholdCents` code defaults, which were also left as-is (still visible in the
  public repo regardless, since the code itself stays).

### Reference cleanup

Fixed dangling references to the deleted docs: `CLAUDE.md` (project-structure tree entry +
removed the "Treasurer's Todo" section), `docs/backlog.md` (intro line + two B-item source
citations + a `~$10.8k` figure that hung off the same deleted doc),
`scripts/backfill-check-numbers.ts` and `scripts/fix-ledger-categories.ts` (console/comment
references to `docs/treasurer-todo.md` / `docs/2026-07-29-clean-fy2025-plan.md`), and three
in-app Treasury User's Guide components that displayed `"see treasurer-todo.md T-nn"` as live UI
text to admins (`guardrails-section.tsx`, `uncashed-checks-section.tsx`,
`zeffy-fund-routing-section.tsx`) — rephrased to drop the dead file reference while keeping the
guidance. Historical narrative mentions of the deleted docs inside old work-log entries and
`docs/decisions.md` were deliberately left as-is (timestamped citations, not live links, and
rewriting dozens of them would be pure busywork outside the task's intent). `.claude/` had no
references to any of the deleted paths.

### Judgment calls (flagging explicitly)

- **`docs/club-constitution-and-bylaws.md`** — kept untouched, including its `$300.00`/`$30.00`/
  `$15.00` entrance-fee and life-membership figures. These are Lions Clubs International's
  standard association-wide fee schedule (the same for every Lions club, not unique to
  Westerville), not "the club's actual books" — treated the same as the IRS filing thresholds.
  Also not on the explicit delete list, and it's already the seed source for a shipped in-app
  feature (`scripts/seed-governance-document.ts`).
- **Per-capita tax rate ($25 LCI + $10.50 District = $71/member/yr)** in
  `docs/features/the-ledger-accounting.md` — left in place for the same reason: an
  association/district-wide standard rate, not Westerville-specific.
- **A handful of illustrative design-reasoning examples** left untouched where the prose itself
  frames the number as hypothetical (e.g. "$2,000 to Westerville Food Pantry" as a mocked-up
  dashboard example in `docs/decisions.md`/`2026-06-25-ledger-impact.md`; "a guardrail warns when
  reserves fall below $1,000" in `2026-07-21-treasury-users-guide.md`, explicitly called out in
  the same sentence as a stale-example risk) — these describe what a feature's UI *would* show,
  not a number from the real books.
- **IRS/legal thresholds** ($250 written-acknowledgment, $75 quid-pro-quo, $50k/$200k/$500k Form
  990 tiers) left everywhere — these are federal law, identical for every nonprofit, not the
  club's own figure.
- **Engineering/code-level constants** ($10,000 reimbursement ceiling, $21.4M Postgres INT4-max
  message, $25,000/$250 `reserveWarnThresholdCents`/`disbApprovalThresholdCents` schema defaults,
  the $100 Activity-fund balance-tolerance default) left in place — these are live application
  defaults that ship in `src/` regardless of this scrub, not exposed "books" data.
- Vitest fixture data in `src/**/*.test.ts` and Playwright fixtures in `e2e/**/*.spec.ts` were
  left untouched throughout — synthetic by construction, out of scope, and several coincidentally
  echo real-looking round numbers (e.g. a `formatDuesAmount(12000) → "$120"` test case) without
  being the club's actual data.

### Verification

- `git grep -InE "\$[0-9]{1,3},[0-9]{3}|\$[0-9]+\.[0-9]{2}"` — reviewed every remaining hit
  individually (274 lines across the repo after the scrub, down from ~1,000+ raw `$[0-9]` matches
  at the start). Every one is a placeholder already containing `X`, `$0.00`, an IRS/legal
  threshold, a code-level engineering constant, an explicitly-synthetic test/QA fixture, the
  bylaws' association-wide fee schedule, or an illustrative example explicitly framed as
  hypothetical in its own sentence. None is a real club figure.
- `git grep -In "Westlund"` — clean, zero hits.
- No dangling references to the five deleted docs anywhere live code/UI text reads, or in
  `.claude/`; historical narrative citations in old work-logs/`decisions.md` intentionally left as
  timestamped record.
- `pnpm exec tsc --noEmit` — clean.
- `pnpm test` — **1466 passed** (78 files), no regressions.
- `pnpm build:only` — production build passes, full route list unchanged.
- **Did not touch production, did not send email, did not create ledger transactions** —
  `PROD_DATABASE_URL` untouched, `RESEND_API_KEY`/`EMAIL_DEV_ALLOWLIST` untouched, no scripts were
  executed against any database during this pass (only read via `git`/`grep`/`sed`/`python3` on
  the working tree).
